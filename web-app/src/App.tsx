import type { Component } from 'solid-js';

import logo from './logo.svg';
import Nav from './Nav';

const App: Component = () => {
  return (
    <Nav onAddRecordClicked={() => { }} />
  );
};

export default App;
